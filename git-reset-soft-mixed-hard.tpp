titl git-reset(1) -- soft, mixed, hard

text \begin{lstlisting}[tagsize=2]
	function git-reset(commit, arg):
	%\hspace{2em}%set HEAD and branch to commit
	%\hspace{2em}%if arg == "--soft":
	%\hspace{4em}%return
	%%
	%\hspace{2em}%update index to commit
	%\hspace{2em}%if arg == "--mixed":
	%\hspace{4em}%return
	%%
	%\hspace{2em}%update working dir to commit
	\end{lstlisting}
