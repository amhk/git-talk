titl git-reset(1) -- examples

item Default values
ite2 commit:
	code HEAD
ite2 arg:
	code --mixed

item
	code git reset --soft
	$\rightarrow$ noop

item
	code git reset --hard
	$\rightarrow$ undo changes to index and working dir

item Demo
