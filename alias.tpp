titl git config --- alias

text Expand to other git command:

item
	code stat = status -s
item
	code logg = log --pretty='format:\%h\%Cgreen\%d\%Creset ...

text Expand to other command:

item
	code foo = !date
item
    code bar = !ls -1 | wc -l
