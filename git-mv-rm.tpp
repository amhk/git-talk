titl git-mv(1), git-rm(1)

syno git mv old new, git rm path

item Similar to the normal mv and rm commands

item Except doing the normal ``stuff'' the git equivalent also operates on the index, i.e. use it for tracked files.
