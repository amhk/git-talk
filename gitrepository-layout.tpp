titl gitrepository-layout(5)

item git version history and index stored in .git directory
ite2
	code git --bare init
	: creates a bare repository, ie only the .git dir

item .git/objects: commit, tree, blob, tag

item Demo
