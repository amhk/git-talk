titl git-cherry-pick(1)

syno git cherry-pick -x commitish

item Duplicate existing commit elsewhere in tree

item Use
	code -x
	by default

item May be accomplished by
	code git format-patch
	and
	code git am
